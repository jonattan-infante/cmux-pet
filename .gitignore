# Swift Package Manager
.build/
.swiftpm/
Package.resolved
*.xcodeproj

# Salida del modo --render (anclado a la raiz: sin la barra inicial tambien
# excluiria Sources/CmuxPetKit/Render/, que es codigo)
/render/

# macOS
.DS_Store

# Python (port de Windows en windows/)
__pycache__/
*.pyc

# Estado local del asistente: nunca va al repo
config.json
voice.json
shell.jsonl
pet.log
pet.pid
